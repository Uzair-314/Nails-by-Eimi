node_modules
dist
.DS_Store
*.local

# Local environment — copy .env.example instead
.env
.env.*
!.env.example

# Design source PDFs — kept locally, not committed (45MB of binaries)
design-reference/

# Admin panel — kept local for now, not published yet.
# App.jsx and adminAvailable.js glob for these, so the storefront builds
# with or without them present.
src/pages/admin/
src/lib/adminApi.js
